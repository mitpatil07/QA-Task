# =========================================================================
# FieldForce Connect Automation — Test Configuration
# =========================================================================
# ⚠️  IMPORTANT: Update the values below before running `mvn test`
#
# This file is read by utils.Config.java at runtime.
# Store sensitive values here (not hard-coded in test classes).
# =========================================================================

# Base URL of the application under test
base.url=https://test.fieldforceconnect.com

# ─── Valid Login Credentials ─────────────────────────────────────────────
# Replace YOUR_PASSWORD_HERE with the password used during sign-up.
# Email: the account registered on the live site
valid.email=mitesh8767@gmail.com
valid.password=YOUR_PASSWORD_HERE

# ─── Login Page ──────────────────────────────────────────────────────────
login.url=https://test.fieldforceconnect.com/auth/login
dashboard.url.fragment=/dashboard

# ─── Attendance Page ─────────────────────────────────────────────────────
attendance.url=https://test.fieldforceconnect.com/attendance

# ─── Expected Messages (update after first run if they differ) ───────────
# Error shown for invalid credentials (captured from Chrome console)
error.invalid.credentials=Invalid Email Id / Mobile No or Password

# Toast text shown after successful punch-in (UPDATE after first run)
toast.punch.in.success=Punched In Successfully
